<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= data.statusCode %> - <%= data.errorTitle %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        padding: 20px;
      }

      h1 {
        color: #333;
      }
    </style>
  </head>
  <body>
    <h1><%= data.statusCode %> - <%= data.errorTitle %></h1>
    <p><%= data.errorMessage %></p>
    <p><a href="/">back</a> to homepage</p>
  </body>
</html>
