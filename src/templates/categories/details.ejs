<main>
  <section class="element-detail">
    <div class="item-info">
      <h2>Category Information</h2>
      <div>
        <strong>Name:</strong>
        <h2 style="display: inline-block;" id="first-name"><%= category.name %></h2>
      </div>
      <% if (currentUser.role==='admin') { %>
        <a class="btn edit" href="/categories/<%= category.id %>/edit">Edit</a>
        <a class="btn delete" href="/categories/<%= category.id %>/delete"> Delete </a>
      <% } %>
      <br>
      <br>
      <div>
        <strong><%= books?.length ? `Books (${ category.bookCount })` : 'There is no book in this category' %></strong>
        <% if (books.length) { %>
          <ul>
            <% books.forEach(book => { %>
              <li>
                <span id="last-name"><a href="/books/<%= book.id %>"><%= book.title %></a></span>
              </li> 
            <% }) %>
          </ul>
        <% } %>
      </div>
      <br />
    </div>
  </section>
</main>
