<main>
  <section class="item-list">
    <h2>Your notifications</h2>
    <br />
    <div class="item-cards">
      <% todoes.forEach(todo => { %>
      <div class="item-card">
        <a href="/todoes/<%= todo.id %>">
          <h3><%= todo.guide.title %></h3>
          <h3><%= `${todo.user.firstName} ${todo.user.lastName}` %> ga</h3>
        </a>
        <br />
      </div>

      <% }) %>
    </div>
  </section>
</main>
